# Older / redundant hypotheticals versions

Moved here 2026-06-22 from the repo root.

| File | Note |
|------|------|
| `hypotheticals.txt` | Text rendering of the 983-item set; same content as `../hypotheticals.json` |
| `hypotheticals_v2.json` | EARLIER v2 batch (503 records, Mar 5) — predates `../hypotheticals_phase10_498.json` (498, richer fields). Distinct, kept for history. |
| `hypotheticals_v2.txt` | Text form of the above |

(The exact-duplicate root `hypotheticals.json` was deleted — it was byte-identical to `../hypotheticals.json`.)
